name: OS and Perl Matrix

on: push

jobs:
  build:
    environment: deployment
    runs-on: ubuntu-latest
    name: Build
    steps:
      - uses: actions/checkout@v2

      - name: View environment
        run: |
          uname -a
          printenv | sort

      - name: Build
        run: |
          echo $SECRET
          echo $SECRET > code.txt
          ls -l
          cat code.txt

